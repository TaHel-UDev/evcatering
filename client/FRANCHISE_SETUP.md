# 🏢 Настройка франчайзинговой системы

## 📋 Структура Directus

### 1. Создайте коллекцию `franchises`

**Settings → Data Model → Create Collection**

```
Название: franchises
Тип: Standard Collection

Поля:
- id (Primary Key, auto-generated)
- name (String) - Название франчайзи (например: "Москва", "Санкт-Петербург")
- subdomain (String, Unique) - Поддомен (например: "msk", "spb")
- logo (Image/File)
- phone (String)
- email (String)
- address (String)
- is_active (Boolean, default: true)
- created_at (Timestamp)
```

### 2. Добавьте поле `franchise_id` в кастомизируемые коллекции

Для каждой коллекции, которую франчайзи может редактировать:

**A) `first_screen`**
```
Добавить поле:
- franchise_id (Many-to-One → franchises) ← ОБЯЗАТЕЛЬНО!
```

**B) `service_formats_block`**
```
Добавить поле:
- franchise_id (Many-to-One → franchises) ← ОБЯЗАТЕЛЬНО!
```

**C) `mission_block`**
```
Добавить поле:
- franchise_id (Many-to-One → franchises) ← ОБЯЗАТЕЛЬНО!
```

**D) `work_block`**
```
Добавить поле:
- franchise_id (Many-to-One → franchises) ← ОБЯЗАТЕЛЬНО!
```

### 3. Глобальные коллекции (БЕЗ franchise_id)

Эти коллекции одинаковые для всех франчайзи:
- `main_page` (SEO, общие настройки)
- `global_header`
- `global_footer`
- `global_why_us_block`
- И другие общие блоки

---

## 🔐 Настройка прав доступа для франчайзи

### Шаг 1: Создайте роль "Franchise Manager"

**Settings → Access Control → Roles → Create Role**

```
Название: Franchise Manager
Описание: Франчайзи с доступом только к своим данным
```

### Шаг 2: Создайте политику доступа

**Settings → Access Control → Policies → Create Policy**

```
Название: Franchise Own Data Only
Связанная роль: Franchise Manager

Права доступа (Permissions):
```

#### A) Для коллекций С franchise_id:

**`first_screen`:**
```
✅ Create: С фильтром franchise_id = $CURRENT_USER.franchise_id
✅ Read: С фильтром franchise_id = $CURRENT_USER.franchise_id
✅ Update: С фильтром franchise_id = $CURRENT_USER.franchise_id
❌ Delete: Нет доступа (или с фильтром)
```

**`service_formats_block`, `mission_block`, `work_block`:**
```
Аналогично first_screen - фильтр по franchise_id
```

**`service_format_card`:**
```
✅ Create, Read, Update: Все права (через связь с service_formats_block)
❌ Delete: По желанию
```

#### B) Для глобальных коллекций:

**`main_page`, `global_header`, `global_footer`:**
```
✅ Read: Только чтение (без фильтров)
❌ Create, Update, Delete: Нет доступа
```

### Шаг 3: Создайте пользователей франчайзи

**User Directory → Create User**

```
Email: moscow@franchise.com
Password: ***
Role: Franchise Manager

Custom Fields:
- franchise_id: 1 (ID Москвы)  ← ВАЖНО!
```

Создайте по пользователю для каждого франчайзи с привязкой к их `franchise_id`.

---

## 🌐 Настройка поддоменов

### Локальная разработка

Добавьте в `/etc/hosts` (Linux/Mac) или `C:\Windows\System32\drivers\etc\hosts` (Windows):

```
127.0.0.1 msk.localhost
127.0.0.1 spb.localhost
127.0.0.1 ekb.localhost
```

Тестируйте на:
- `http://msk.localhost:3000`
- `http://spb.localhost:3000`

### Продакшен

**Настройте DNS:**
```
A msk.yourdomain.com → IP сервера
A spb.yourdomain.com → IP сервера
A *.yourdomain.com → IP сервера (wildcard для всех поддоменов)
```

**Nginx/Apache:** Настройте wildcard для всех поддоменов → Next.js приложение.

---

## 🎨 Visual Editor для франчайзи

### Настройка Visual Editor в Directus

**Settings → Visual Editor → Create New**

Создайте **отдельную конфигурацию для каждого франчайзи:**

**Конфигурация 1 - Москва:**
```
Name: Москва (msk)
URL: https://msk.yourdomain.com
(или http://msk.localhost:3000 для локальной разработки)
```

**Конфигурация 2 - Санкт-Петербург:**
```
Name: Санкт-Петербург (spb)
URL: https://spb.yourdomain.com
```

**Конфигурация 3 - Екатеринбург:**
```
Name: Екатеринбург (ekb)
URL: https://ekb.yourdomain.com
```

### Как это работает

Каждый франчайзи видит и редактирует **только свои данные**:

1. **Франчайзи заходит в Directus** под своей учёткой (franchise_id привязан к пользователю)
2. **Открывает Visual Editor** → выбирает конфигурацию **своего города**
3. **Directus применяет права доступа** с фильтром `franchise_id = $CURRENT_USER.franchise_id`
4. **API возвращает только те данные**, к которым у пользователя есть доступ
5. **Visual Editor показывает** только те элементы, которые франчайзи может редактировать
6. **Глобальные блоки** видит, но **не может редактировать** (read-only права)

### ⚠️ Важно!

- Питерский франчайзи **физически не может** редактировать московские данные
- Даже если он откроет `msk.yourdomain.com`, API вернёт 403 Forbidden
- Права доступа работают на уровне Directus API, а не фронтенда
- Это **безопасная** изоляция данных

### Инструкция для франчайзи

```markdown
# Как редактировать сайт

1. Откройте https://admin.yourdomain.com
2. Войдите под своим логином
3. Нажмите "Visual Editor" в левом меню
4. Выберите конфигурацию с ВАШИМ городом
5. Кликайте на элементы для редактирования
6. Сохраните изменения
```

---

## 📊 Пример структуры данных

### Franchises:
```json
[
  { "id": 1, "name": "Москва", "subdomain": "msk" },
  { "id": 2, "name": "СПб", "subdomain": "spb" },
  { "id": 3, "name": "Екатеринбург", "subdomain": "ekb" }
]
```

### First Screen (для каждого франчайзи):
```json
[
  {
    "id": 1,
    "franchise_id": 1,
    "title": "Кейтеринг в Москве",
    "subtitle": "Лучший сервис столицы"
  },
  {
    "id": 2,
    "franchise_id": 2,
    "title": "Кейтеринг в СПб",
    "subtitle": "Северная кухня"
  }
]
```

---

## ✅ Преимущества этого подхода

✅ **Один Directus** - проще управлять и дешевле  
✅ **Безопасность** - франчайзи видят только свои данные  
✅ **Гибкость** - можно добавлять новых франчайзи без изменения кода  
✅ **Visual Editor** - работает для каждого франчайзи  
✅ **Общий контент** - можно обновлять глобальные блоки централизованно  
✅ **Масштабируемость** - легко добавлять новые регионы  

---

## 🚀 Следующие шаги

1. ✅ Создайте коллекцию `franchises` в Directus
2. ✅ Добавьте поле `franchise_id` во все кастомизируемые коллекции
3. ✅ Заполните данные для тестовых франчайзи
4. ✅ Настройте роли и права доступа
5. ✅ Создайте пользователей для франчайзи
6. ✅ Протестируйте на локальных поддоменах
7. ✅ Разверните на продакшен с wildcard DNS

---

## 🆘 Нужна помощь?

Если возникнут вопросы при настройке - обращайтесь! 🛠️

