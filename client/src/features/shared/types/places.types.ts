export interface PlacesData {
  id: number;
  name: string;
  description: string;
  area: number;
  capacity: number;
  adress: string;
  full_description: string;
  preview: { id: string };
  banner: { id: string };
}